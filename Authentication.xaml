<Window x:Class="UserDesktopApp.Authentication"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:UserDesktopApp"
        mc:Ignorable="d"
        Title="User Application" Height="450" Width="400">
    <Grid Background="Bisque">
        <Border MinWidth="200" Margin="15" Background="Gainsboro" VerticalAlignment="Center" Padding="30" MaxWidth="350" 
                CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" Color="Gray" ShadowDepth="0"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Logging in!" FontSize="30" FontWeight="Normal" Margin="5"/>
                
                <Grid Margin="0 15 0 15 ">
                    <Button HorizontalAlignment="Right" Content="Registration" Click="RegistrationButton"/>
                    
                </Grid>
                <TextBox x:Name="TextBoxLogin" materialDesign:HintAssist.Hint="Enter login" Style="{StaticResource MaterialDesignFloatingHintTextBox}"/>
                <PasswordBox x:Name="TextBoxPassword" materialDesign:HintAssist.Hint="Enter password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}"/>
                
                <Button Content="Log in" Margin="0 40" Click="ClickToAuthenticate"/>
            </StackPanel>
        </Border>
        
    </Grid>
</Window>